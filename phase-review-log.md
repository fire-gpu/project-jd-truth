# Phase Review 日志

> 每个 Phase 完成时的结构化 Review。格式固定，控制简洁。

---

## Phase 1: 调研阶段

- **完成日期**：2026-04-17
- **产出**：
  - JD 样本 21 条（6 岗位类别，来源 BOSS 直聘/智联）
  - 竞品分析报告（16 个竞品/替代方案）
  - 调研结论报告（含 MVP 功能建议）
- **关键决策**：
  1. 解读报告采用分维度架构：核心维度（必有）+ 扩展维度（按需）+ 个性化维度（有背景时）
  2. 用户背景输入选方案 C（极简可选版，一个文本框）
  3. 岗位类别 6 类：前端/后端/产品/运营/设计/数据分析
  4. 明确不做：简历优化、面试模拟、岗位搜索
- **发现的问题**：无重大问题
- **遗留项**：无
- **工程思路**：调研驱动决策——先收集真实 JD 验证场景存在，再通过竞品分析确认市场空白，最后确定功能范围。避免"先做再想"。

---

## Phase 2: 知识库构建

- **完成日期**：2026-04-18
- **产出**：
  - 12 条经验种子数据（6 岗位 × 2 来源：访谈模拟 + 社区整理）
  - 3 个 JSON 文件：frontend-backend / product-operations / design-data
  - 数据结构定义：data-schema.md
- **关键决策**：
  1. JSON 文件存储（Demo 阶段零部署成本）
  2. 关键词匹配 + 岗位类别映射（后续升级轻量 RAG）
  3. 每岗位 2 来源保证视角多样性
- **发现的问题**：
  1. ~~[已修复] 跨文件 ID 重复（exp_001~004 在 3 个文件中重叠）~~
  2. ~~[已修复] level 枚举值"掌握"不合规~~
  3. ~~[已修复] tech-architecture.md 与 data-schema.md 结构不一致~~
  4. [待修复] 种子数据 source_type 需诚实标注（interview→ai_synthesized_interview）
  5. [待修复] 种子数据缺 keywords/tags 字段，影响检索精度
  6. [待修复] 合成数据的"系统性平庸"问题——缺少真正反直觉的洞察
- **遗留项**：
  - 轻量 RAG 实现（FAISS 内存索引 + Embedding API）
  - 幻觉边界规则执行（修改种子数据 + 注入 Prompt）
  - 真实数据收集计划启动
- **工程思路**：先建数据再建检索。数据结构预留 UGC 和向量检索扩展位。JSON 存储适合 Demo 阶段的快速迭代。辩论后认识到"关键词匹配"在语义鲁棒性上不足，需升级为轻量 RAG。

---

## Phase 2.5: Review & 修正（辩论后）

- **完成日期**：2026-04-18
- **产出**：
  - REVIEW-report.md（综合评分 6.8/10）
  - DEFENSE-phase2.md / DEBATE-opposition.md（正反方辩论稿）
  - hallucination-boundaries.md（幻觉边界 5 条硬规则）
  - CHANGELOG.md + phase-review-log.md（Review 机制建立）
- **关键决策**：
  1. 辩论裁决：接受反方观点，需做轻量 RAG + 幻觉边界
  2. 种子数据诚实标注：source_type 改为 ai_synthesized_* 前缀
  3. Review 机制：CHANGELOG（日常）+ phase-review-log（Phase 级别），不常规生成长文档
- **发现的问题**：
  1. [待修复] 12 条合成数据与"真实从业者经验"价值主张存在信任鸿沟
  2. [待修复] 缺少 JSON→Prompt 格式化层（浪费 30-40% token）
- **遗留项**：
  - 种子数据诚实标注修改（source_type + source_detail + data_verification）
  - keywords/tags 字段补充
  - 轻量 RAG 实现
- **工程思路**：通过正反方辩论暴露盲点，比单人 Review 更有效。建立轻量级变更追踪机制，确保每个决策可回溯。幻觉边界是产品可信度的底线，必须在 Prompt 层面硬编码。

---

## Phase 3: Prompt 工程

- **完成日期**：2026-04-18
- **产出**：
  - V1 Prompt 模板（v1-jd-parser.md）：8 维度分层输出 + 幻觉边界 5 规则 + 12 项质量自检
  - 轻量 RAG 模块（knowledge-retriever.js）：关键词 0.6 + Jaccard 语义 0.4，49 项测试通过
  - Prompt 构建器（prompt-builder.js）：模板加载 + 占位符替换 + 元数据返回
  - 格式化层：压缩率 76.1%（5537→1325 字符）
  - 质量评估（prompt-test-results.md）：V1 基线 21/25
- **关键决策**：
  1. Jaccard + bigram 分词作为 V1 轻量语义方案（不依赖外部 Embedding API）
  2. 混合排序权重：关键词 0.6 + 语义 0.4
  3. 相关性阈值 MIN_HYBRID_SCORE = 0.1
- **发现的问题**：
  1. ~~[已修复] Prompt 注入防护~~：_sanitizeInput() 长度限制 + 5 种注入模式过滤
  2. ~~[已修复] 检索相关性阈值~~：低分经验不注入
  3. ~~[已修复] 模板提取容错~~：三级回退策略
- **遗留项**：
  - 时间占比幻觉约束（V2 改进）
  - 边界测试补充（空 JD、超长 JD）
  - prompt-iteration-log.md
- **工程思路**：Prompt 工程的核心不是"写 Prompt"，而是"构建数据到 Prompt 的管道"。格式化层节省 76% token 是意外收获。幻觉边界必须在代码层（输入清洗）和 Prompt 层（规则约束）双重保障。

---

## Phase 4: 开发实现

- **完成日期**：2026-04-18
- **产出**：
  - 后端 API 服务（Express）：6 个接口，Mock+AI 双模式，零外部依赖
  - AI 对接层（llm-client.js）：OpenAI 兼容格式，超时/重试/降级
  - 微信原生小程序：3 个页面 + API 封装，17 个文件
  - 免费 LLM API 调研：推荐智谱 GLM-4-Flash（永久免费）
- **关键决策**：
  1. Mock+AI 双模式：未配置 API Key 时自动降级为 Mock，保证前端可独立开发
  2. 智谱 GLM-4-Flash 作为推荐 LLM（永久免费、200K 上下文、OpenAI 兼容）
  3. 微信原生小程序（非 UniApp），减少框架复杂度
  4. 内存存储（Demo 阶段不需要数据库）
- **发现的问题**：
  1. ~~[已修复] api.js 响应判断逻辑错误~~：code→success
  2. ~~[已修复] report.js 期望结构化数据但后端返回 Markdown~~：改为 Markdown→HTML 渲染
  3. ~~[已修复] app.js 岗位类别 ID 与后端不匹配~~：统一为后端格式
  4. ~~[已修复] index.wxml 动态渲染确认无需修改~~
- **遗留项**：
  - 获取智谱 API Key 并测试 AI 模式
  - 小程序真机调试
  - 速率限制（防滥用）
  - Markdown 渲染增强（表格、代码块支持）
- **工程思路**：前后端并行开发效率高但对接风险大——Review 暴露了 4 个致命问题，说明并行开发必须加强接口契约管理。Mock 模式是前后端解耦的关键设计。AI 对接层的降级机制保证了系统在任何情况下都能提供基本服务。

---

## Phase 5: 测试验证 + 真机修复

- **完成日期**：2026-04-19
- **产出**：
  - 真机测试反馈 5 个问题，全部修复（UI 改版、流式输出、Prompt 优化、卡顿优化）
  - 边界测试 8 用例（7 通过 / 1 失败 / 1 警告），详见 test-boundary-results.json
  - AI 模式端到端验证：产品经理 JD 2519 字完整报告，8 维度全部输出
  - 路线 B 功能优化规划（roadmap-b.md）：导出方案 + UI 优先级 + 功能优先级
- **关键决策**：
  1. 流式输出方案选 WebSocket（非 SSE），因微信小程序不支持 ReadableStream
  2. 增量渲染→全文渲染：Markdown 转换是上下文相关的，增量必然格式错乱
  3. 产品命名：JD 说真话 + 玫红色 #FF2D55
  4. 报告导出推荐：分享海报（canvas 图片）+ 复制到剪贴板，不做 DOCX/PDF
- **边界测试结果**：
  - ✅ 参数校验（4/4）：空 JD、超长 JD、无效类别、超长背景全部正确拦截
  - ✅ 安全防护（2/2）：XSS/模板注入未执行、Prompt 注入被 _sanitizeInput 过滤
  - ⚠️ 功能降级（1/2）：无背景时差距分析章节标题仍存在（内容正确标注"无法分析"）
  - ⚠️ 安全警告：AI 在 JD 潜台词中引用了原始恶意文本（未执行，但视觉上不友好）
- **AI 模式测试结果**（产品经理 JD）：
  - 报告长度：2519 字（V1 只有 318-455 字就截断）
  - 维度完整性：8/8 全部输出
  - 格式正确性：标题层级、列表、JD 潜台词引用+箭头格式正确
  - 幻觉检查：未编造公司名/人名/薪资
  - 数据来源声明：包含
  - 知识库注入：2 条产品运营经验
- **并发问题分析**：
  - 智谱 API 有并发限制（通常 1-2 个同时请求），多用户同时解读会触发 429
  - Demo/参赛阶段（1-2 人）无影响，公开上线需加请求队列或充值
- **遗留项**：
  - 无背景时 Prompt 动态跳过差距分析章节
  - _sanitizeInput 增强清洗 script 标签和模板语法
  - 前端提交按钮冷却（防连点）
  - 多岗位 AI 测试（产品经理通过，其他 4 岗位因 API 限流未完成）
- **工程思路**：增量渲染是经典的"过早优化"陷阱——Markdown 转换是上下文相关的，增量必然格式错乱。全文渲染 + 300ms 节流（5ms 执行时间）完全不是瓶颈。边界测试暴露了"功能正确"和"格式正确"的区别——AI 输出了差距分析标题但标注了"无法分析"，功能上合理但严格不满足"不包含该维度"的预期。

---

## Phase 6: 上线部署（云开发改造）

- **完成日期**：2026-04-21
- **产出**：
  - 微信云开发改造：后端逻辑从 Express + WebSocket 迁移到云函数（parseJD）
  - 云函数部署成功，真机测试通过，代码审核通过
  - Prompt V3.1 精简：删除冗余"输出规则"段落，行动建议去掉推荐资源，Prompt 字符数减少 28%
  - UI 优化：自定义 loading 遮罩（淡橙色 #FFA03C 旋转动画 + 暗色卡片）
  - 前端改造：WebSocket 流式输出 → 云函数一次性返回 + localStorage 历史记录
  - .gitignore 创建（防止 API Key 和 node_modules 泄露）
- **关键决策**：
  1. 放弃自建服务器 + 备案域名方案（成本 ~184 元/年 + 1-2 周备案等待），改用微信云开发（0 成本、即开即用）
  2. 代价：失去 WebSocket 流式输出，改为 loading + 一次性展示报告（用户可接受）
  3. 知识库数据内嵌到云函数 index.js（tcb CLI 不上传 JSON 文件，require 方式不可行）
  4. API Key 直接写在代码中（代码不公开，无需环境变量配置）
- **部署过程中解决的问题**：
  1. ~~[已修复] project.config.json 缺少 miniprogramRoot/cloudfunctionRoot~~
  2. ~~[已修复] 云函数入口函数名：main_handler → main~~
  3. ~~[已修复] 知识库 JSON 内嵌语法错误（JSON.parse 字符串转义问题）~~
  4. ~~[已修复] Node.js 兼容性：catch {} → catch (e)、??/?. → 三元表达式~~
  5. ~~[已修复] tcb fn deploy 不上传 JSON 文件 → 改为对象字面量内嵌~~
  6. ~~[已修复] 前后端分页参数不匹配（page/pageSize vs limit）~~
  7. ~~[已修复] history.js 缺少 goToIndex 方法~~
  8. ~~[已修复] report.js 分享标题 jobCategory 未赋值~~
- **部署架构**：
  ```
  改造前：小程序 → WebSocket → 自建 Node.js 服务器 → 智谱 API
  改造后：小程序 → 云函数 parseJD → 智谱 API
  ```
- **费用**：0 元（云开发基础版免费额度 + GLM-4-Flash 永久免费）
- **遗留项**：
  - ~~小程序审核通过后正式发布~~
  - **审核失败**：个人主体不支持 AI 深度合成服务类目，需企业主体
  - 路线 B 功能优化（分享海报、复制到剪贴板）
  - 多岗位 AI 测试覆盖
- **工程思路**：云开发改造的核心挑战不是代码逻辑（直接从 server.js 提取），而是部署环境的差异（Node 版本、文件上传、入口函数名）。调试过程中反复遇到"本地正常但云端报错"的问题，根因是部署工具的行为与预期不符。教训：先确认部署环境的行为，再写代码。

---

## Phase 7: 审核与发布尝试

- **完成日期**：2026-04-22
- **产出**：
  - 代码上传成功（Upload v1.0.0）
  - 提交审核，获得审核结果
  - 真机测试截图（Weixin image）已保存
- **审核结果**：**失败**
  - 原因 1：小程序服务内容涉及深度合成技术（AI 问答），个人主体尚未开放该服务类目，需申请企业主体类型小程序
  - 原因 2：小程序备注不符合个人性质，涉及企业或经营性情况
- **关键结论**：
  1. **个人小程序无法发布 AI 问答类产品**，这是微信平台的政策限制，非代码问题
  2. 解决方案：注册企业主体小程序（需营业执照）或寻找其他分发渠道
  3. 当前状态：Demo 可在开发者工具和真机 Preview 模式下正常运行，代码审核通过（代码本身没问题）
- **实际可交付物**：
  - ✅ 完整可运行的小程序代码（真机 Preview 可用）
  - ✅ 云函数已部署（AI 解读功能正常）
  - ✅ 真机测试截图
  - ❌ 无法通过微信搜索获取（需企业主体）
- **遗留项**：
  - 注册企业主体小程序后重新提交审核
  - 或探索其他分发方式（如提供源码、内测分发等）
